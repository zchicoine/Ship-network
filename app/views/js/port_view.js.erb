
var temp_port = '<%= escape_javascript( @saved_name )%>'

// make sure the map is in place
if($("#googleMap").length == 0){

    $("#outer-map").html(" <%= escape_javascript(render 'google_map/index')%>");
    var spec = {'new_map':true,'layer':PORT_LEVEL, 'port_name':temp_port };
    reload_the_map_with_specification(spec);
    update_port_view(temp_region);

}else{

    update_port_view(temp_port);
}


