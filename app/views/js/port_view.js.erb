
var port_name = '<%= escape_javascript( @saved_name )%>'
var port_coordinates = ['<%= escape_javascript(  port_coordinates[:latitude] )%>','<%= escape_javascript(  port_coordinates[:longitude] )%>'];
var region_name = '<%= escape_javascript( region_name )%>'
// make sure the map is in place
if($("#googleMap").length == 0){

    $("#outer-map").html(" <%= escape_javascript(render 'google_map/index')%>");
    var spec = {'new_map':true,'layer':PORT_LEVEL, 'region_name': region_name,'port_name':port_name };
    reload_the_map_with_specification(spec);



}
MainViewGeneratorInstance.portView(port_name,port_coordinates);


